baseURL: 
# baseURL: https://jiang068.github.io/lab_reports/
# 如果是用户页面仓库（仓库名是 用户名.github.io），则用：
# baseURL: https://你的用户名.github.io/
languageCode: zh-cn
DefaultContentLanguage: zh-cn
title: 实验报告合集
theme: hugo-theme-stack

# 设置hasCJKLanguage为true以正确处理中文内容
hasCJKLanguage: true

# 分页设置
pagination:
  pagerSize: 10

# 永久链接配置
permalinks:
    post: /:slug/
    page: /:slug/

relativeURLs: true
canonifyURLs: false

# 语言设置
languages:
    zh-cn:
        languageName: 简体中文
        title: 实验报告合集
        weight: 1

module:
    hugoVersion:
        extended: true
        min: "0.87.0"

params:
    mainSections:
        - post
    featuredImageField: image
    rssFullContent: true
    favicon:

    # 背景图片透明度设置 (0.0-1.0，值越小越透明，0.0为全透明，1.0为完全不透明)
    backgroundOpacity:
        page: 0.4          # 页面整体背景透明度
        sidebar: 0.0        # 侧边栏透明度 (0.0 = 全透明)
        widget: 0.0         # 小工具透明度 (0.0 = 全透明)
        toc: 0.85           # 目录(TOC)小工具透明度
        article: 0.4       # 文章卡片透明度
        overlay: 0.3        # 背景遮罩层透明度 (0.0 = 无遮罩, 1.0 = 全黑)
        # 深色模式
        dark:
            page: 0.2
            sidebar: 0.0
            widget: 0.0
            toc: 0.85       # 深色模式目录透明度
            article: 0.2
            overlay: 0.5    # 深色模式遮罩透明度

    footer:
        since:
        customText:

    dateFormat:
        published: Jan 02, 2006
        lastUpdated: Jan 02, 2006 15:04 MST

    sidebar:
        compact: false
        emoji: 📚
        subtitle: jiang068
        avatar:
            enabled: true
            local: false
            src: https://avatars.githubusercontent.com/u/169352191?v=4
        
        # 自定义侧边栏内容
        title: "说明"
        description: |
          这里收集了本人的各类课程的实验报告、代码示例和参考资料。
          所有资料仅供参考，请勿直接抄袭。
          原始数据文件保存在仓库 /static/data 文件夹中。
        
        # 链接列表
        linksTitle: "快速链接"
        links:
            - name: "本站 github 地址"
              url: "https://github.com/jiang068/lab_reports/"
              external: true
            - name: "cloudflare 加速镜像站"
              url: "https://ds.fufu.blog/"
              external: true  # 外部链接
            # - name: "链接 3"
            #   url: "#"
            #   external: false 内部链接
        
        # 自定义文字卡片
        cards:
            - title: "📢 通知"
              content: "这是通知。"
            # - title: "💡 提示"
            #   content: "支持 **Markdown** 格式哦!"

    article:
        headingAnchor: false
        math: true
        toc: true
        readingTime: true
        license:
            enabled: false
            default: Licensed under CC BY-NC-SA 4.0

    comments:
        enabled: false
        provider: disqus

        disqusjs:
            shortname:
            apiUrl:
            apiKey:
            admin:
            adminLabel:

        utterances:
            repo:
            issueTerm: pathname
            label:

        beaudar:
            repo:
            issueTerm: pathname
            label:
            theme:        

        remark42:
            host:
            site:
            locale:

        vssue:
            platform:
            owner:
            repo:
            clientId:
            clientSecret:
            autoCreateIssue: false

        # Waline client configuration see: https://waline.js.org/en/reference/client/props.html
        waline:
            serverURL:
            lang:
            visitor:
            avatar:
            emoji:
                - https://cdn.jsdelivr.net/gh/walinejs/emojis/weibo
            requiredMeta:
                - nick
                - mail
            locale:
                admin: Admin
                placeholder:

        twikoo:
            envId:
            region:
            path:
            lang:

        giscus:
            repo:
            repoID:
            category:
            categoryID:
            mapping:
            strict:
            lightTheme:
            darkTheme:
            reactionsEnabled: 1
            emitMetadata: 0
            inputPosition:
            lang:

        gitalk:
            owner:
            admin:
            repo:
            clientID:
            clientSecret:

        cusdis:
            host:
            id:

    widgets:
        homepage:
            - type: search
            - type: archives
              params:
                  limit: 5
            - type: categories
              params:
                  limit: 10
            - type: tag-cloud
              params:
                  limit: 10
        page:
            - type: toc

    opengraph:
        twitter:
            # Your Twitter username
            site:

            # Available values: summary, summary_large_image
            card: summary_large_image

    defaultImage:
        opengraph:
            enabled: false
            local: false
            src:

    colorScheme:
        # Display toggle - 隐藏深色/浅色模式切换按钮
        toggle: false

        # Available values: auto, light, dark - 强制使用深色模式
        default: dark

    imageProcessing:
        cover:
            enabled: true
        content:
            enabled: true

### Custom menu
menu:
    main: []
    social: []

related:
    includeNewer: true
    threshold: 60
    toLower: false
    indices:
        - name: tags
          weight: 100
        - name: categories
          weight: 200

markup:
    goldmark:
        renderer:
            unsafe: true
    tableOfContents:
        endLevel: 4
        ordered: true
        startLevel: 2
    highlight:
        noClasses: false
        codeFences: true
        guessSyntax: true
        lineNos: true
        lineNumbersInTable: true
        tabWidth: 4
